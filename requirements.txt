# requirements.txt — Asistencia ICA Backend (Python/FastAPI)

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client (reemplaza fetch de Node)
httpx>=0.27.0

# PDF generation
reportlab>=4.2.0

# PDF merging (preop 2 páginas)
pypdf>=4.2.0

# Data validation
pydantic>=2.7.0

# Form data (flow-confirmation x-www-form-urlencoded)
python-multipart>=0.0.9

# Timezone support (formulario RM timestamp)
tzdata>=2024.1
